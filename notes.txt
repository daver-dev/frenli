May want to consider turborepo for when there are multiple nested projects in this repo.

To do:

profile page with mock data
    -profile pic
    -bio
    -pics
    -method for only grabbing x number of most recent pics, and adding to pics array
        when scrolled to the bottom (same as below method with optional profile argument)

feed page with mock data
    -pics
    -method for only grabbing x number of most recent pics, and adding to pics array
        when scrolled to the bottom

settings page

messages page with mock data

