# java-backend

Java (Spring Boot) backend, deployed as a normal always-on app on a single EC2
instance. The instance is started only when testing (a few hours a month) and
stopped otherwise to keep cost near zero.

Data model: see [DATA_MODEL.md](../DATA_MODEL.md) at the repo root (shared across backends).

Not yet implemented.
